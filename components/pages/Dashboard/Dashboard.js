import './Dashboard.css';
import { WrapperCards } from '../../shared/WrapperCards/WrapperCards';

export const Dashboard = () => {

    return `
        <div>
            ${WrapperCards()}
        </div>
    `
}